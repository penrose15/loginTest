spring:
  datasource:
    hikari:
      jdbc-url: jdbc:h2:mem:testdb
      driver-class-name: org.h2.Driver
      username: sa
  h2:
    console:
      path: /h2
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  data:
    redis:
      host: localhost
      port: 6379


jwt:
  secret: sdfghjkwertyuidcfghjkertyuifghjh
  key: jwt
  access-token-expiration-minutes: 10
  refresh-token-expiration-minutes: 180

